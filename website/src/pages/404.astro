---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="404 — dding-dong"
  description="Page not found"
  lang="en"
>
  <main class="min-h-screen flex flex-col items-center justify-center px-6 text-center">
    <p class="font-gmarket text-(length:--text-h1) font-bold text-hobak leading-none mb-6">
      404
    </p>
    <h1 class="text-2xl font-semibold text-meok mb-2">
      <span class="not-found-en">Page not found</span>
      <span class="not-found-ko hidden">페이지를 찾을 수 없습니다</span>
    </h1>
    <p class="text-onggi text-sm mb-8 max-w-sm">
      <span class="not-found-en">The page you're looking for doesn't exist.</span>
      <span class="not-found-ko hidden">찾고 있는 페이지가 존재하지 않습니다.</span>
    </p>
    <a
      href={import.meta.env.BASE_URL}
      class="inline-flex items-center gap-2 px-6 py-3 rounded-full bg-hobak text-hanji font-semibold hover:bg-hobak-deep transition-colors duration-200"
    >
      <span class="not-found-en">Go Home</span>
      <span class="not-found-ko hidden">홈으로 가기</span>
    </a>
  </main>
</Layout>

<script is:inline>
  (function() {
    // eslint-disable-next-line no-var -- intentional: is:inline bypasses module bundling
    var isKo = window.location.pathname.indexOf('/ko/') !== -1
      || navigator.language.startsWith('ko');
    if (isKo) {
      document.querySelectorAll('.not-found-en').forEach(function(el) { el.classList.add('hidden'); });
      document.querySelectorAll('.not-found-ko').forEach(function(el) { el.classList.remove('hidden'); });
      document.documentElement.lang = 'ko';
    }
  })();
</script>
